[pytest]
python_files = test_*.py *_test.py
norecursedirs = .git .* build dist CVS _darcs {arch} *.egg node_modules venv __pycache__
# Add any other directories you want to exclude, like Ai or ai if they are duplicates or not for testing
# For now, focusing on the python_files pattern.
